import {Layout} from "widgets/layout";
import {Develop} from "widgets/develop";

/** Страница правил (в разработке). */
export const Rules = () => {
	return (
		<Layout>
			<Develop />
		</Layout>
	)
}
