import {Layout} from "widgets/layout";
import {Develop} from "widgets/develop";

/** Страница создания поста (в разработке). */
export const CreatePost = () => {
	return (
		<Layout>
			<Develop />
		</Layout>
	)
}
